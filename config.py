# Thresholds for edge creation
RST_THRESHOLD = 0.3
CAUSAL_THRESHOLD = 0.27

# Weights for similarity components in causal inference
WEIGHTS = {
    "semantic": 0.35,
    "context": 0.30,
    "metadata": 0.2,
    "identity": 0.1,
    "rst": 0.05
}

# Output paths
OUTPUT_FOLDER = "outputs"

